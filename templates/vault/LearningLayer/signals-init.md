---
month: '{month}'
type: learning-signals
---

# LearningLayer Signals — {month}

Domain-agnostic observations. Signals only, never work content.

This file was seeded by `weave-cli init` on {date} to document the
install itself as the first signal in this vault. Rename or replace it
once real signals start accumulating — remember the naming convention
in BOOT.md's Conventions block: `signals-<YYYY-MM>[-<machine-suffix>].md`,
so concurrent writers never collide on the same file.

## Install notes
- Vault scaffolded by `weave-cli init` at {date}.
- Org Team engine installed alongside (see `[[entity-Team-Hub]]`).
- No project content yet — this vault is a fresh, generic starting point.
